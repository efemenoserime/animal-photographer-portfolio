image: node:latest

cache:
  paths:
    - .cache/
    - .node_modules/

stages:
  - pages

pages:
  stage: pages
  script:
    - npm ci
    - ./node_modules/gatsby/bin/gatsby build
  artifacts:
    paths:
      - public
  only:
    - master
